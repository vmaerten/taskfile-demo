version: 3

tasks:
  build:
    requires:
      vars:
        - BINARY
    sources:
      - '**/*.go'
    generates:
      - '{{.BINARY}}'
    cmds:
      - echo "build from a remote taskfile"
      - go build
